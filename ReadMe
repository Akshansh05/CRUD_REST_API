. clone the repository . go to the dirctory .install node .install mongodb .install dependencies and express .install cors .run mongodb server .run server.js .install postman api tester .set the required parameter and give the data inputs in JSON format(for post& update ) in the body of postman and set the content as applicatopn/json . send the request and you will get the response .CONGO your API has been created and Tested .happy Coding
